<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Teste</title>
</head>

<body>
    <h1>Testando o arquivo ejs</h1>
    <hr>
    <h2>
        <%= nome %>
            <%= linguagem %>
    </h2>

    <% if(msg){%>
        <h3>Isso é uma mensagem de erro</h3>
        <%} else {%>
            <h3>Não tem nenhum erro</h3>
            <% } %>

                <% produtos.forEach(produto=> { %>
                    <div>
                        <h2>
                            <%= produto.nome %>
                        </h2>
                        <%= produto.preco %>
                    </div>
                    <% }); %>
</body>

</html>